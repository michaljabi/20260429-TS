// Tak można zaimportować jedynie plik — posiadający tzw. default export:
// W tym układzie, to my importując plik — decydujemy, jak się będzie nazywało to, co importujemy spod `default export`.
import application from "./module-1";

// startujemy aplikację:
application();
